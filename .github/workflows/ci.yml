name: Sprint 0 Check

on:
  push:
    branches: [ main, rescue/sprint-0-complete ]
  pull_request:

jobs:
  json-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate JSON data
        run: npm run check:json
