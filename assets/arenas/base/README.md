# EPIC 69 — Map base assets

Runtime paths are declared in `data/world/world_map_v4.json`.

The ten curated 1920×1080 map bases are preserved in ChatGPT Library under:
`/CRIA DO TATAME/Visual Canon v2/EPIC69 Maps/`.

Their exact SHA-256 digests and expected repo paths are recorded in:
`data/visual/map_ui_asset_intake_v1.json`.

Until the binary PNG files are ingested through a connector/action that accepts local binary files, the runtime deliberately falls back to a dark placeholder while keeping all nodes, locks and panels interactive.

Do not mark these assets `shipping=true` without binary ingestion, sidecars, art QA and rights review.
